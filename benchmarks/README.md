# Benchmarks

Starter benchmark scripts for extra credit load testing.

Important:

- These files are scaffolds only.
- Do not run benchmarks through the Pygame UI.
- Final tests should call data structures/services directly.
- Compare optimized structures against brute-force baselines.

Suggested plots:

- x-axis: dataset size or query volume
- y-axis: response time in milliseconds
- optional y-axis: memory usage

TODO(BENCHMARKS): Add generated CSV output and final graph images for the report.
