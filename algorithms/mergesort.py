from __future__ import annotations

"""Merge sort skeleton.

Expected complexity:
- time: O(n log n)
- extra space: O(n)

Use cases:
- catalog sorting
- match history sorting
- leaderboard/report comparisons
"""


def mergesort(records: list[object], key=None, reverse: bool = False) -> list[object]:
    # TODO(MERGESORT): Implement recursively or iteratively and benchmark.
    _ = (records, key, reverse)
    raise NotImplementedError("Team must implement mergesort.")
