// Game instance manager header scaffold.
//
// TODO(C++): Define GameInstance, create_session, join_session, end_session,
// and lookup_session. Keep networking protocol documented for Python.

#pragma once

class GameInstanceManager {
public:
    void create_session();
    void join_session();
    void end_session();
};
