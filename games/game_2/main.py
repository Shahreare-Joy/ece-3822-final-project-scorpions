from __future__ import annotations

"""Game 2 placeholder.

TODO(GAME 2): Implement this team member's game and keep the public entry point
as run_game(player) so the arcade launcher can call it.
"""


def run_game(player: object = None) -> dict[str, object]:
    _ = player
    return {"ok": False, "message": "Game 2 is a placeholder and is not connected yet."}
