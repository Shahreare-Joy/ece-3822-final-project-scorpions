# Avatar Assets

Optional player avatar images go here.

Suggested naming:

```text
avatar_joy.png
avatar_maya.png
avatar_ren.png
```

TODO(AVATAR): Add a future avatar selection screen or account setting. For now,
mock players store an `avatar_id`, and the UI falls back to a colored circle if
no image file exists.
