from __future__ import annotations


def run_game(player_info: dict[str, object] | None = None, session_info: dict[str, object] | None = None) -> dict[str, object]:
    """Example placeholder entry point.

    This folder shows teammates where a future game can live. The arcade launch
    registry intentionally marks Sky Raiders as not connected, so this function
    is not called yet.
    """

    _ = (player_info, session_info)
    return {"ok": False, "message": "Sky Raiders is not connected yet."}

