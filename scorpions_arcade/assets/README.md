# Assets

Static UI assets for the Pygame client live here.

Avatar images should go under:

```text
scorpions_arcade/assets/avatars/
```

The profile screen can use a player `avatar_id` to look for an image such as
`avatar_joy.png`. If no image is present, the UI falls back to a colored
placeholder circle.
